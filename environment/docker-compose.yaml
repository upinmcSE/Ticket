version: '3.2'

networks:
  pre-event-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:

services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    image: ticket-app:local
    container_name: ticket-app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      SPRING_DATA_REDIS_HOST: ticket-redis
      SPRING_DATA_REDIS_PORT: "6379"
      SPRING_DATASOURCE_URL: jdbc:postgresql://ticket-postgres:5432/ticket
      SPRING_DATASOURCE_USERNAME: postgresql
      SPRING_DATASOURCE_PASSWORD: postgresql
      JAVA_OPTS: "-XX:MaxRAMPercentage=75.0 -XX:+ExitOnOutOfMemoryError"
    networks:
      - pre-event-network
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep -q '\"status\":\"UP\"' || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 40s

  postgres:
    image: postgres:16-alpine
    container_name: ticket-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ticket
      POSTGRES_USER: postgresql
      POSTGRES_PASSWORD: postgresql
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - pre-event-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgresql -d postgresql"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ticket-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data
    networks:
      - pre-event-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      
#  kafka:
#    image: apache/kafka:3.7.0
#    container_name: ticket-kafka
#    ports:
#      - "9094:9094"   # external — Spring Boot (host) connects here
#    environment:
#      KAFKA_NODE_ID: 1
#      KAFKA_PROCESS_ROLES: broker,controller
#      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:9094
#      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://ticket-kafka:9092,EXTERNAL://localhost:9094
#      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
#      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
#      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
#      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@ticket-kafka:9093
#      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
#      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
#      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
#      KAFKA_NUM_PARTITIONS: 3
#      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
#      KAFKA_LOG_RETENTION_MS: 604800000
#    healthcheck:
#      test: [ "CMD", "/opt/kafka/bin/kafka-topics.sh", "--list", "--bootstrap-server", "localhost:9092" ]
#      interval: 5s
#      timeout: 10s
#      retries: 10
#    networks:
#      - pre-event-network
#
#  kafka-ui:
#    image: provectuslabs/kafka-ui:latest
#    container_name: ticket-kafka-ui
#    ports:
#      - "8989:8080"   # open http://localhost:8989
#    depends_on:
#      kafka:
#        condition: service_healthy
#    environment:
#      KAFKA_CLUSTERS_0_NAME: pre-event
#      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: ticket-kafka:9092
#      DYNAMIC_CONFIG_ENABLED: "true"
#    networks:
#      - pre-event-network